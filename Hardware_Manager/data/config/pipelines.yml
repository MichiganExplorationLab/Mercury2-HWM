## Contains the settings for the available hardware pipelines.
## 
## This file defines the settings for the hardware pipelines. For a complete description of the available settings, 
## please reference the included configuration.

# Define the pipelines
pipelines:
  - id: "test_pipeline"
    hardware:
      - device_id: "device_1"
  - id: "test_pipeline_2"
    hardware:
      - device_id: "device_2"
