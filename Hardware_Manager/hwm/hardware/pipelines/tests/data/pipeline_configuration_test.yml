# This configuration file contains a valid pipeline configuration and is used to test the pipeline manager

pipelines:
  - id: "test_pipeline"
    hardware:
      - device_id: "device_1"
  - id: "test_pipeline_2"
    hardware:
      - device_id: "device_2"
