# This configuration file contains an invalid device configuration and is used to test the device manager. The
# "Test_Driver" driver is assumed to exist in the HWM drivers package.

devices:
  - device_id: "test_device"
    driver_class: "Test_Driver"
    settings:
      - address: "test"
