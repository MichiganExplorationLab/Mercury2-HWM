# This configuration file contains an invalid device configuration (contains a duplicate device) and is used to test the
# device manager. The "Test_Driver" driver is assumed to exist in the HWM drivers package.

devices:
  - device_id: "test_device"
    driver: "Test_Driver"
    settings:
      - address: "test"
  - device_id: "test_device_2"
    driver: "Test_Driver"
    settings:
      - address: "test"
  - device_id: "test_device"
    driver: "Test_Driver"
    settings:
      - address: "test"