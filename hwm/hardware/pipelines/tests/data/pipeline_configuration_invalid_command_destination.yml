# This configuration file contains a invalid pipeline configuration and is used to test the PipelineManager and
# Pipeline classes.

pipelines:
  - id: "test_pipeline"
    mode: "transceive"
    description: "Invalid because it specifies a setup command for a device not used by the pipeline."
    hardware:
      - device_id: "test_device"
        pipeline_input: true
        pipeline_output: true
      - device_id: "test_device2"
    setup_commands:
      - command: "station_time"
        destination: "system"
      - command: "some_command"
        destination: "test_device3"
  - id: "test_pipeline2"
    mode: "receive"
    hardware:
      - device_id: "test_device"
        pipeline_output: true
